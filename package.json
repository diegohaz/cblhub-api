{
  "name": "opencbl-parse",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.js",
  "scripts": {
    "postinstall": "npm run setup && npm run build",
    "setup": "mkdir -p cloud",
    "clean": "rm -rf cloud",
    "build": "browserify src/main.js -t babelify -o cloud/main.js",
    "deploy": "npm run build && parse deploy",
    "test": "npm run build && mocha ./test/test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/diegohaz/opencbl-parse"
  },
  "author": "Diego Haz",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/diegohaz/opencbl-parse/issues"
  },
  "homepage": "https://github.com/diegohaz/opencbl-parse",
  "devDependencies": {
    "babelify": "^6.3.0",
    "browserify": "^11.0.1"
  },
  "dependencies": {
    "moment": "^2.10.6",
    "underscore": "^1.8.3",
    "underscore.string": "^3.2.2"
  }
}
